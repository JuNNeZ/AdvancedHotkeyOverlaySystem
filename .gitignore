# Editor settings are local, but .vscode/scripts holds the verification
# scripts the release workflow runs, so that directory must stay tracked.
/.vscode/*
!/.vscode/scripts/

AdvancedHotkeyOverlaySystem.code-workspace
/build/
